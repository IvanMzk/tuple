cmake_minimum_required(VERSION 3.2)
set(project_name tuple)
project(${project_name} LANGUAGES CXX)
if (CMAKE_PROJECT_NAME STREQUAL PROJECT_NAME)
    add_subdirectory(test)
endif()
add_subdirectory(src)